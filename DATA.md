# 数据说明

本仓库**不附带任何原始数据**。论文与实验所用的数据集涉及第三方版权，不能公开分发；
请按下面的约定自行准备数据，代码不需要改动即可直接跑。

---

## 1. 代码需要什么样的数据

| 要求 | 说明 |
|---|---|
| 文件格式 | Excel（`.xlsx` / `.xls`，取第一张工作表或 `--sheet` 指定）或 `csv` |
| 行列结构 | 每行一个时间点，按时间**升序**排列，等间隔采样 |
| 采样间隔 | 默认 15 分钟、每天 96 点（可用 `--points-per-day` 改） |
| 目标列 | 至少一列是待预测的功率值，用 `--target-col` 指定；不指定时取最后一列 |
| 特征列 | 除目标列外的**所有数值列**自动作为候选输入特征 |
| 时间列 | 可选。给一列时间戳（非数值列，自动识别）后，会额外生成年内日期的正余弦特征 |
| 缺失值 | 允许存在，程序会做前向/后向填充并在日志里提示 |
| 数据长度 | 至少 `look_back + horizon + 1` 行；按默认设置（96+4）建议不少于 30 天 |

论文实验用的那份数据形态：**19583 行 × 3 列、15 分钟分辨率、约 204 天**，
目标列夜间恒为 0、正午前后达峰（典型光伏出力曲线）。
为规避版权风险，这里只描述形态，不提供文件，也不提供列名映射。

---

## 2. 最快上手

把数据放到 `data/dataset.xlsx`，然后：

```bash
python run_train.py                          # 目标列默认取最后一列
python run_train.py --data D:/mydata.xlsx --target-col power --time-col time
```

常用数据相关参数：

```
--data            数据文件路径，默认 data/dataset.xlsx
--sheet           工作表名
--target-col      目标列名，默认最后一列
--time-col        时间戳列名（用于年内日期特征与多表合并）
--points-per-day  每天采样点数，默认 96
--no-time-of-day  不添加日内时刻正余弦特征
```

---

## 3. 引入更多信息：IFS / GFS 数值天气预报等

### 方式一：拼成一张表（不改代码）

把预报量按相同时间戳对齐后，作为额外列写进主表即可。所有数值列都会自动成为输入特征：

| time | power | temp | cloud | t2m_fc | ssrd_fc | wind10_fc |
|---|---|---|---|---|---|---|
| 2024-01-01 00:00 | 0.0 | 3.1 | 0.8 | 2.9 | 0 | 1.4 |
| 2024-01-01 00:15 | 0.0 | 3.0 | 0.8 | 2.9 | 0 | 1.5 |

```bash
python run_train.py --data power_with_nwp.xlsx --target-col power --time-col time
```

### 方式二：多文件自动合并（一条命令，不改代码）

主表只放实测数据，预报单独放 csv/xlsx，用时间列左连接：

```bash
python run_train.py \
  --data power.xlsx --target-col power --time-col time \
  --extra-files gfs_forecast.csv ifs_forecast.xlsx \
  --merge-on time
```

- `--extra-files` 支持多个文件，按 `--merge-on`（默认等于 `--time-col`）左连接主表；
- 合并进来的数值列自动成为特征；
- 与主表重名的列会自动加 `_nwp` 后缀；
- 日志里会打印合并列数与**时间戳匹配率**，匹配不上的部分按前向/后向填充补齐。

对应代码在 `src/data.py::_merge_extra_files`，异常信息会直接指出缺哪一列、哪个文件不存在。

### 方式三：自定义对齐或派生逻辑

需要更复杂的处理时，改这几处即可：

| 位置 | 作用 |
|---|---|
| `src/data.py::load_dataframe` | 读表、列名清洗、非数值列处理、缺失值填充 |
| `src/data.py::_merge_extra_files` | 额外数据（预报）的合并入口 |
| `src/data.py::_resolve_columns` | 目标列与特征列的确定，以及重复列检查 |
| `src/data.py::_add_time_features` | 自定义时间特征（太阳高度角、时角、节假日等） |
| `src/data.py::prepare_data` | 合并 → 切分 → 归一化 → 滑窗的总流程 |

也可以只通过配置挑列，便于做特征消融：

```python
from src.data import DataConfig, prepare_data

cfg = DataConfig(
    path="power.xlsx", target_col="power", time_col="time",
    feature_cols=["power", "ghi", "temp", "cloud", "tod_sin", "tod_cos"],
)
bundle = prepare_data(cfg)
```

> `feature_cols` 显式给出时，程序只使用这些列（目标列与时间列除外），方便逐个特征对比。

### 多时效预报（lead time）

若要用 T+1h / T+2h 的预报，把它们**做成多列**（如 `ssrd_1h, ssrd_2h, ssrd_3h`），
每一行代表"在预测时刻已经拿到的那一组预报"，而不是未来时刻才有的值。

---

## 4. 避免预报泄漏（重要）

引入 NWP 预报时最常见的错误，是把"事后才知道的观测"当成"当时可得的预报"。请保证：

1. 特征里只放**预测时刻之前已经发布**的预报；
2. 预报更新频率低于数据频率时（例如一天只更新几次），
   要按"最近一次已发布的预报"回填，而不是按"最近一次可用的数值"；
3. 不要用同期的**实测**辐照度、温度冒充预报特征，那等价于把答案提前给模型；
4. 若预报与实测来自不同时区，先统一到同一时区再对齐。

程序无法自动判断这类语义泄漏，需要你在准备数据时保证时间对齐的含义正确。
数据层面能自动检查的只有：重复列、与目标完全相同的列、缺失值、时间列可否解析。

---

## 5. 程序会自动做的数据检查

跑起来后日志会打印这些提示（`results/<tag>/data_meta.json` 里也有留存）：

- 列名重复、缺失值数量、被忽略的非数值列；
- 时间列是否可以解析为日期；
- 特征列之间是否**完全重复**（自动去重）；
- 某个特征列是否与目标列**逐点完全相同**
  （会提示：这等价于把目标自身的历史作为自回归输入。若原始数据缺少真正的驱动变量，请补上辐照度、温度等列）；
- 归一化只使用**训练段**拟合，测试段不参与，避免信息泄漏。

---

## 6. 目录约定

```
data/           # 把你的数据放在这里
data/dataset.xlsx   # --data 的默认路径（需要你自己提供）
```

`.gitignore` 已经忽略 `data/` 下的表格文件，避免误把原始数据提交上去。
